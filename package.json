{
  "name": "metalsmith-demo",
  "version": "0.0.3",
  "description": "A Simple demo for MetalSmith",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js && http-server build -p 80 -a localhost -o -c-1"
  },
  "author": "Russell Dempsey <SgtPooki@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "handlebars": "2.0.0",
    "metalsmith": "1.3.0",
    "metalsmith-templates": "0.6.0"
  },
  "devDependencies": {
    "metalsmith-markdown": "0.2.1"
  }
}
